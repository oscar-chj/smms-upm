/**
 * Auth-related type definitions
 * Re-exports from user.types.ts for backward compatibility
 */

export type { User, AuthResult, LoginCredentials } from "./user.types";
export { UserRole } from "./user.types";
